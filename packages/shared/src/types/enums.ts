export enum Role {
  COUPLE = 'COUPLE',
  VENDOR = 'VENDOR',
  ADMIN = 'ADMIN',
}

export enum VendorStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
  SUSPENDED = 'SUSPENDED',
}

export enum InquiryStatus {
  NEW = 'NEW',
  CONTACTED = 'CONTACTED',
  CONFIRMED = 'CONFIRMED',
  CLOSED = 'CLOSED',
}
