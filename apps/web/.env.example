# Copy to .env.local and fill in. Next.js reads .env files from this directory (apps/web).
NEXT_PUBLIC_API_URL="http://localhost:4000/api/v1"
# Same value as GOOGLE_CLIENT_ID in apps/api/.env (safe to expose to the browser).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""
