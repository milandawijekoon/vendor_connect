name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  deploy:
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_SSH_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          # `docker compose build` can exceed the 10m default on a cold cache
          # (e.g. after a lockfile change invalidates the pnpm install layer).
          command_timeout: 30m
          script: |
            set -euo pipefail
            cd /opt/vendor-connect
            git fetch origin main
            git reset --hard origin/main
            docker compose -f docker-compose.prod.yml build
            docker compose -f docker-compose.prod.yml up -d --remove-orphans
            docker compose -f docker-compose.prod.yml exec -T api ./node_modules/.bin/prisma migrate deploy --schema=./prisma/schema.prisma
            docker compose -f docker-compose.prod.yml exec -T api pnpm run reindex
            docker image prune -af --filter "until=48h"
